<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hlq.androidroad_01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".egg.Egg1Activity" />
        <activity android:name=".study.TableLayoutActivity" />
        <activity android:name=".study.ButtonActivity" />
        <activity android:name=".study.StackViewActivity" />
        <activity android:name=".study.AdapterViewFlipperActivity" />
        <activity android:name=".study.ZoomButtonActivity" />
        <activity android:name=".study.ToggleButtonActivity" />
        <activity android:name=".study.SwitchActivity" />
        <activity android:name=".study.ExpandableListViewActivity"></activity>
    </application>

</manifest>